#Что делать если у Вас:
#
#Сделать архив и перекинуть файлы 
#tar -cf archive-name.tar *
# scp archive-name.tar root@192.168.100.2:/
#tar -xvf archive-name.tar

#сломался RAID
#заходим с паролем
sudo mdadm --stop /dev/md0
sudo mdadm --remove /dev/md0
sudo mdadm --zero-superblock /dev/sdb
sudo mdadm --zero-superblock /dev/sdc
nano /etc/fstab
#удалить содержимое
/etc/mdadm.conf
#удалить содержимое
#перезагрузить
